[
    {{- range $i, $c := (.Site.GetPage "section" "cities").Sections -}}
    {{ if $i }},{{ end }}
    {
        "name": "{{ .Title }}",
        "slug": "{{ replace .Params.url "/" "" }}"
    }
    {{- end }}
]